# Supabase Setup (Deprecated)

Supabase setup is no longer required. The application has been migrated to use native local PostgreSQL for user authentication and case library management. All user profiles and investigations are now managed directly by the FastAPI backend using standard PostgreSQL database tables.
