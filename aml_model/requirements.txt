# Pinned to match the versions in the root requirements.txt where the same
# package is shared with backend/phase6-8, to avoid resolver drift when both
# are installed into the same environment.
scikit-learn==1.5.2
pandas==2.2.3
numpy==2.1.3
scipy==1.14.1
joblib==1.4.2
matplotlib==3.9.2
seaborn==0.13.2
xgboost==2.1.3
imbalanced-learn==0.12.4
openpyxl==3.1.5
